1.Mvvm 是指双向数据流，即 View-Model 之间的双向通信，由 ViewModel 作桥接
2.而单向数据流则去除了 View -> Model 这一步，需要由用户手动绑定。
3.Angluar 早期的脏检测机制开创了 mvvm 先河，监听效率虽然和 getter 相当，
    但在代码中修改对象需要手动调用，不是很方便，而当 watch 数据时，需要 N + 1 次确认数据是否有联动变化
4.现在几乎所有框架都改为 getter/setter 劫持实现监听，任何数据的变化都可以在一个事件循环周期内完成